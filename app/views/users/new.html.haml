%h2 Sign up for SeatYourself

- if @user.errors.any?
  %div.errors
    %h3 Please fix the following errors:
    %ul
      - @user.errors.full_messages.each do |e|
        %li= e

= form_for(@user) do |f|

  %div
    = f.label :name
    = f.text_field :name

  %div
    = f.label :email
    = f.text_field :email

  %div
    = f.label :password
    = f.password_field :password

  %div
    = f.label :password_confirmation
    = f.password_field :password_confirmation

  %div
    = f.submit 'Sign up!'